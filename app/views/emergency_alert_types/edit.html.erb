<%= link_to("<<Back to list", {:action=>"list"}) %>
<div >
<h2>Update Emergency Alert info</h2>
<%= form_for(:emergencyalerttype, :url=>{:action=>"update", :id=>@emergencyalerttype.id}) do |f| %>


<%= render(:partial=>"form", :locals=>{:f=>f}) %>



<p> * fields marked with asterisk are compulsary fields</p>


	<div>

	<%=submit_tag("Update Emergency Alert info") %>

	</div>
<% end -%>
</div>

<div style="color:red">
	<% @emergencyalerttype.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>