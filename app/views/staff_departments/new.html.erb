<%= link_to("<<Back to full list", {:action=>"list"}, :class=>"back-links") %>
<div class="scategory new">
<h2>Create staff Department</h2>
<%= form_for(:scategory, :url=>{:action=>"create"}) do |f| %>




	<%= render(:partial=>"tabletofill", :locals=>{:f=>f}) %>


	<p> * fields marked with asterisk are compulsary fields</p>

		<div class="form_buttons">

		<%=submit_tag("Create staff department") %>

		</div>
<% end -%>
</div>

<div id="errors" style="color:red">
	<% @scategory.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>