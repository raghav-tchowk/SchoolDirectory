<%= link_to("<<Back to list", {:action=>"list"}) %>
<div>
<h2>Update Competition Alert info</h2>
<%= form_for(:compalerttype, :url=>{:action=>"update", :id=>@compalerttype.id}) do |f| %>


<%= render(:partial=>"form", :locals=>{:f=>f}) %>



<p> * fields marked with asterisk are compulsary fields</p>


	<div>

	<%=submit_tag("Update Competition Alert info") %>

	</div>
<% end -%>
</div>

<div style="color:red">
	<% @compalerttype.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>