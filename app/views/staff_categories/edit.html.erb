<%= link_to("<<Back to list", {:action=>"list"}, :class=>"back-links") %>
<div class="staff Update">
	<h2>Update staff categories</h2>

	<%= form_for(:staff, :url=>{:action=>
		"update", :id=>@staff.id} ) do |f| %>


	<%= render(:partial=>"tabletofill", :locals=>{:f=>f}) %>



	<p> * fields marked with asterisk are compulsary fields</p>


	<div class="form_buttons">

	<%=submit_tag("Update staff info") %>

	</div>
<% end -%>
</div>

<div id="errors" style="color:red">
	<% @staff.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>