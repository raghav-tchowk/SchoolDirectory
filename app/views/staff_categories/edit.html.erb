<%= link_to("<<Back to list", {:action=>"list"}) %>
<div>
	<h2>Update scat categories</h2>

	<%= form_for(:scat, :url=>{:action=>
		"update", :id=>@scat.id} ) do |f| %>


	<%= render(:partial=>"form", :locals=>{:f=>f}) %>



	<p> * fields marked with asterisk are compulsary fields</p>


	<div>

	<%=submit_tag("Update scat category info") %>

	</div>
<% end -%>
</div>

<div style="color:red">
	<% @scat.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>