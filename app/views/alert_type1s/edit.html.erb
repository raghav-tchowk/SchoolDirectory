<%= link_to("<<Back to list", {:action=>"list"}) %>
<div>
<h2>Update Alert info</h2>
<%= form_for(:alerttype1, :url=>{:action=>"update", :id=>@alerttype1.id}) do |f| %>


<%= render(:partial=>"form", :locals=>{:f=>f}) %>



<p> * fields marked with asterisk are compulsary fields</p>


	<div>

	<%=submit_tag("Update alert info") %>

	</div>
<% end -%>
</div>

<div style="color:red">
	<% @alerttype1.errors.full_messages.each do |err| %>
    	*<%= err %><br />
    <% end %>
</div>