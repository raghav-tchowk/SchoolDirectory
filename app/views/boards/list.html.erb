<% if !flash[:notice].to_i.blank? %>
<div>
<%= flash[:notice] %>
</div>
<% end -%>
	

<div>
	<h2>Boards</h2>
	<table summary="boards list">
		<tr>
			<th>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</th>
			<th>Name&nbsp;&nbsp;&nbsp;&nbsp;</th>
			<th>Full Name&nbsp;&nbsp;&nbsp;&nbsp;</th>
		</tr>
		
		<% @boards.each do |board| %>

			<tr>
				<td><%=board.id%></td>
				<td><%=board.name%></td>
				<td><%=board.full_name%></td>
				
				<td>

					<%=link_to("Show", {:action=>"show", :id=>board.id}) %>
				<%= link_to("Edit", {:action=>"edit", :id=>board.id}) %>
				<%= link_to("Delete", {:action=>"delete", :id=>board.id}) %>
				</td>
			</tr>
		<% end %>
	</table>
</div>

<%= link_to("<<new Board>>", {:action=>"new"}) %>

